image: node:18.15

stages:
  - dev

cache:
  paths:
    - node_modules/

backend-lint:
  stage: dev
  script:
    - cd application/
    - npm run lint
  tags:
    - shared-fi

backend-build:
  stage: dev
  script:
    - cd application/
    - npm run build
  tags:
    - shared-fi

backend-test:
  stage: dev
  script:
    - cd application/
    - npm run test
  tags:
    - shared-fi

frontend-lint:
  stage: dev
  script:
    - cd back-end/
    - npm run lint
  tags: 
    - shared-fi

frontend-build:
  stage: dev
  script:
    - cd back-end/
    - npm run build
  tags: 
    - shared-fi

frontend-test:
  stage: dev
  script:
    - cd back-end/
    - npm run test
  tags:
    - shared-fi